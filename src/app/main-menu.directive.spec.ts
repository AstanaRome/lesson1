import { MainMenuDirective } from './main-menu.directive';

describe('MainMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new MainMenuDirective();
    expect(directive).toBeTruthy();
  });
});
